import { useState } from "react";
import { createFileRoute } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { ImageNotice, SectionHeading } from "@/components/techguru";
import { gallery } from "@/lib/techguru-content";

export const Route = createFileRoute("/gallery")({ head: () => ({ meta: [{ title: "Gaming Gallery | TECHGURU Marondera" }, { name: "description", content: "Preview the atmosphere planned for TECHGURU gaming experiences. Real venue photos will be added when supplied." }, { property: "og:title", content: "Gaming Gallery | TECHGURU Marondera" }, { property: "og:description", content: "See illustrative VR, PlayStation and gaming event imagery for TECHGURU." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: GalleryPage });
function GalleryPage() { const [filter, setFilter] = useState("ALL"); const shown = filter === "ALL" ? gallery : gallery.filter((item) => item.category === filter); return <section className="section-shell min-h-screen pt-32"><SectionHeading eyebrow="THE ATMOSPHERE" title="SEE TECHGURU IN ACTION." copy="Real TECHGURU venue photography has not yet been supplied. Every image below is clearly marked as an illustrative placeholder." /><div className="mt-8 flex flex-wrap gap-2">{["ALL", "VR", "PLAYSTATION", "VENUE", "EVENTS"].map((item) => <Button key={item} size="sm" variant={filter === item ? "default" : "outline"} onClick={() => setFilter(item)}>{item}</Button>)}</div><div className="mt-10 columns-1 gap-4 sm:columns-2 lg:columns-3">{shown.map((item, index) => <figure key={`${item.category}-${index}`} className="relative mb-4 break-inside-avoid overflow-hidden border border-border"><img src={item.image} alt={item.alt} width={1600} height={1200} loading="lazy" className={index % 3 === 0 ? "aspect-[4/5] w-full object-cover" : "aspect-[4/3] w-full object-cover"} /><ImageNotice /><figcaption className="absolute right-3 top-3 bg-background/80 px-2 py-1 text-[10px] font-black text-primary backdrop-blur">{item.category}</figcaption></figure>)}</div></section>; }