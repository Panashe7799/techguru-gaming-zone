import { createFileRoute, Link } from "@tanstack/react-router";
import { MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { SectionHeading } from "@/components/techguru";
import { messages, pricing, whatsappUrl } from "@/lib/techguru-content";

export const Route = createFileRoute("/pricing")({ head: () => ({ meta: [{ title: "Gaming Session Pricing | TECHGURU" }, { name: "description", content: "View TECHGURU session categories and ask for current gaming prices on WhatsApp." }, { property: "og:title", content: "Gaming Session Pricing | TECHGURU" }, { property: "og:description", content: "VR, PlayStation, group and event session pricing at TECHGURU." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: PricingPage });
function PricingPage() { return <section className="section-shell min-h-screen pt-32"><SectionHeading eyebrow="SESSION OPTIONS" title="CHOOSE YOUR EXPERIENCE." copy="Prices have not yet been supplied. Ask TECHGURU on WhatsApp for current session details." /><div className="mt-12 grid gap-5 sm:grid-cols-2">{pricing.map((item, index) => <article key={item.name} className={index === 0 ? "pricing-card border-primary" : "pricing-card"}><p className="text-xs font-black uppercase text-primary">0{index + 1}</p><h2 className="mt-8 font-display text-2xl font-black uppercase">{item.name}</h2><p className="mt-3 text-sm text-muted-foreground">{item.detail}</p><p className="mt-9 font-display text-4xl font-black">{item.price}</p><div className="mt-7 flex flex-wrap gap-2"><Button asChild><Link to="/contact" hash="booking">BOOK NOW</Link></Button><Button asChild variant="outline"><a href={whatsappUrl(messages.booking)} target="_blank" rel="noreferrer"><MessageCircle /> ASK ON WHATSAPP</a></Button></div></article>)}</div></section>; }